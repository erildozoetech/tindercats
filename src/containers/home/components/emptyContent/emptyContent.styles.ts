import { StyleSheet } from "react-native";


const styles = StyleSheet.create({
  container: {
    top: 120
  },
  text: {
    fontWeight: '700',
    fontSize: 126,
    color: '#BFBFC0'
  }
});

export default styles;