import { IconsTypePropTypes } from "../../../../globalComponents/IconSvg/iconSvg.types";

export interface ISwitchPropTypes {
  isFavorite: boolean;
}

export interface IIconPropTypes {
  name: IconsTypePropTypes;
  isSelected: boolean;
}